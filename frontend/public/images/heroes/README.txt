Drop the top-of-page background photos here using these exact filenames
(the pages already reference these paths, so a new file appears automatically
on refresh — no code changes needed):

  home.jpg       -> Home page hero
  about.jpg      -> About Us page hero
  products.jpg   -> Products page hero
  events.jpg     -> Events page hero
  careers.jpg    -> Careers page hero
  contact.jpg    -> Contact page hero

Recommended: JPG, landscape, at least 1920x900px, under ~400KB.
Until a file is added, the section just shows the current dark navy
background (no broken-image icon), so it's safe to add these one at a time.

If you'd rather use .png or .webp, update the matching '--hero-img' path
in the page's .jsx file (frontend/src/pages/) to match.
